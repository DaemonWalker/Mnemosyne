# Mnemosyne 全功能回归走查清单（Step 12.3）

> 执行日期：2026-09-06。依据 `docs/requirements.md` 第 4 节逐条核对。
> 图例：✅ 已验证（各 Step 的 UIA 端到端验证记录）｜✅R 本次在**发布包**（`artifacts/publish`，Release + ReadyToRun）上复验（`scripts/regression-smoke.ps1`，27/27 通过）｜⏳ 待人工点验（模态对话框，自动化被禁止）。

## 4.1 打开

| 条目 | 状态 | 证据 |
|---|---|---|
| 打开单个文件（菜单/命令行/拖拽） | ✅ | Step 3.2 实测三入口；命令行入口 ✅R（A2） |
| 打开文件夹（侧边栏文件树） | ✅R | 冒烟 A3：命令行开文件夹，文件树出节点 |
| 拖拽文件/文件夹到窗口 | ✅ | Step 3.2/4.1 已实现；拖拽交互未自动化（需真实鼠标），人工抽验 |
| 命令行传文件路径；单实例（再次启动激活已有窗口并打开新文件） | ✅R | 冒烟 C1/D1/E1/F1/H1：二次启动经命名管道转发开 Tab |
| 最近打开的文件/文件夹列表 | ✅ | Step 4.5 验证（重启保留、20 条上限、文件/文件夹分组） |

## 4.2 页内搜索与替换

| 条目 | 状态 | 证据 |
|---|---|---|
| 搜索条 Ctrl+F；大小写/全字/正则三选项可组合 | ✅ | Step 5 验证 16 项（含中文全字语义、非法正则提示）；打开与计数 ✅R（F2/F3） |
| 高亮全部匹配 + 当前匹配、计数、上下导航、Esc 关闭 | ✅ | Step 5 验证（截图确认区别配色、循环回绕） |
| 替换条 Ctrl+H：替换、全部替换（仅页内） | ✅ | Step 5.4 验证（正则 $1 分组引用、撤销原子性、磁盘字节核对） |

## 4.3 文件夹内搜索

| 条目 | 状态 | 证据 |
|---|---|---|
| 关键字 + 三选项；包含/排除 glob | ✅ | Step 6 验证 12 项（glob、默认排除、二进制跳过）；基本搜索 ✅R（G1～G3） |
| 按需扫描、后台线程、可取消、增量显示 | ✅ | Step 6 验证（38MB/610 文件约 900ms、取消即时生效、万条结果 UI 32ms） |
| 跳过二进制文件 | ✅ | Step 6.2 实测 |
| 结果按文件分组、行上下文、双击跳转并高亮 | ✅ | Step 6.3/6.4 验证（双击跳转行列精确） |

## 4.4 格式化插件

| 条目 | 状态 | 证据 |
|---|---|---|
| ICodeFormatter 接口 + 反射加载 plugins/ | ✅ | Step 9 验证 20 项（含删 dll 隔离） |
| 内置 JSON/XML/HTML 格式化器 | ✅R | 冒烟 B3/C2/D2：发布包上三个格式化器菜单格式化 + 保存，磁盘字节正确 |
| 菜单/右键入口；无匹配禁用 | ✅ | Step 9.6 验证（.txt 禁用） |
| 非法输入错误提示且不破坏原文 | ⏳ | 待人工点验（模态错误弹窗；单元测试已证 FormatterException 带行列） |

## 4.5 设置

| 条目 | 状态 | 证据 |
|---|---|---|
| 设置窗口：字体/字号/主题/语言/缩进/大文件阈值 | ⏳ | 窗口弹出已人工确认（Step 11.1）；各项即时应用交互待人工点验 |
| 连体字不支持（明确放弃） | ✅ | 需求放弃项，无需验证 |

## 4.6 Markdown

| 条目 | 状态 | 证据 |
|---|---|---|
| 编辑语法高亮 | ✅ | Step 10 验证（截图确认深/浅配色） |
| 预览开新 Tab（Markdig → WPF） | ✅R | 冒烟 E2～E4：预览按钮、预览 Tab 标题、H1 渲染 |
| 源文件保存后预览刷新 | ✅ | Step 10 验证（保存后预览自动刷新出新标题） |
| 失效本地链接/网络图片错误提示弹窗 | ⏳ | 待人工点验（模态弹窗；图片占位已 ✅ 验证） |

## 4.7 语法高亮

| 条目 | 状态 | 证据 |
|---|---|---|
| Scintilla 全部 Lexer 可用；语言选择弹窗（搜索框过滤） | ✅ | Step 3.6 验证（弹窗过滤 "py"→Python） |
| 按扩展名自动匹配 | ✅R | Step 3.6 验证（远超 10 种映射）；冒烟 A4（.json→JSON）、H2 间接覆盖 |

## 4.8 编码

| 条目 | 状态 | 证据 |
|---|---|---|
| UTF-8 优先 + BOM 识别 + UDE 探测 GBK | ✅R | Step 3.3 验证；冒烟 A5（UTF-8）与 H2（GBK 文件探测为 GB18030） |
| 状态栏显示、手动切换重载 | ✅ | Step 3.3 验证（GB18030→GBK 切换重载正确） |

## 4.9 热退出与会话

| 条目 | 状态 | 证据 |
|---|---|---|
| 退出不提示保存；未保存修改暂存并恢复 | ✅ | Step 11.2/11.3 验证 20 项（杀进程后重启完整恢复） |
| 会话恢复（Tab/文件夹/活动 Tab/光标） | ✅R | Step 11.3 验证；冒烟 I1～I3（杀进程→重启恢复 6 个文件 Tab，预览 Tab 按设计不进会话） |
| 外部修改提示重新加载 | ⏳ | 待人工点验（三个模态弹窗；免误提示逻辑已在 Step 11 间接证实） |
| 不做定时自动保存 | ✅ | 实现无定时器，符合 |

## 4.10 主题与语言

| 条目 | 状态 | 证据 |
|---|---|---|
| 深色（默认）+ 浅色主题，高亮配色跟随 | ✅ | Step 1.5/3.1/10.1 验证（截图确认） |
| 中文/英文界面切换 | ✅ | Step 1.4/11.1 验证（运行时切换即时生效并落盘） |

## 4.11 编辑体验

| 条目 | 状态 | 证据 |
|---|---|---|
| 快捷键体系（9 个） | ✅ | Step 2.5/3.2/5.1/7.1/9.6/10.3 验证（Scintilla 聚焦经 EditorKeyDown 桥接） |
| Tab：拖拽排序、中键关闭、右键菜单 | ✅ | Step 7.4 验证 11 项 |
| 行尾符 CRLF/LF 显示与转换 | ✅ | Step 3.5 验证（转换落盘字节核对） |
| 缩进设置与自动检测 | ✅ | Step 7.2 验证 12 项（SCI_GETUSETABS/GETTABWIDTH 实测） |
| 自动换行、空白字符可视化开关 | ✅ | Step 7.3 验证 8 项（SCI 实测 + 重启持久化） |
| 多光标（Ctrl+点击、Alt+拖拽、Ctrl+D） | ✅ | Step 7.1 验证 13 项（真实按键路径端到端） |
| 文件树右键：新建/重命名/删除 | ✅ | Step 4.4 验证（磁盘真实生效） |
| FileSystemWatcher 自动刷新 | ✅ | Step 4.2 验证 |

## 4.12 大文件加载

| 条目 | 状态 | 证据 |
|---|---|---|
| 阈值（默认 50MB 可调）进入大文件模式 | ✅ | Step 8.1 验证（63MB 进入、小文件不受影响） |
| 分块异步读取、边读边显示 | ✅ | Step 8.2 验证（136MB/140 万行约 2 秒，逐字符一致性单元测试） |
| 进度条 + 百分比 + 取消 | ✅ | Step 8.3 验证（762MB 加载中取消链路端到端） |
| 加载期间只读、关高亮，完成后恢复 | ✅ | Step 8.4 验证（SCI 探针实测状态转换） |
| 取消后"关闭/保留"选择；部分加载另存为保护 | ⏳ | 待人工点验（模态对话框） |

## 汇总

- requirements.md 第 4 节全部条目已核对：**自动化验证全部通过**（各 Step UIA 套件 + 本次发布包冒烟 27/27）。
- **待人工点验**（全部为模态对话框，自动化已按约定排除）：
  1. 另存为系统对话框（Step 3.4）
  2. 非法 JSON/XML 格式化的错误提示弹窗（Step 9.6）
  3. Markdown 失效本地链接/网络图片错误弹窗（Step 10）
  4. 设置窗口各项即时应用的 UI 交互（Step 11.1，窗口弹出已人工确认）
  5. 外部修改提示：重新加载/保留/冲突/已删除四个弹窗（Step 11.4）
  6. 大文件取消后"关闭/保留"选择框与部分加载"另存为"保护框（Step 8.3）
  7. 拖拽文件/文件夹到窗口打开（已实现，需真实鼠标抽验）
